# 📚 刷课脚本 使用说明

## 简介

自动检测网课页面的「当前任务已达到完成条件」弹窗并精准点击「下一个」，自动检测并点击「播放」按钮，**支持课程系统音频（扬声器内录）自动分小节录制**。内嵌多尺度模板匹配、智能几何播放拓扑识别与双轨切课定位，彻底杜绝误点「上一个」、文字笔画误判与未完成时误跳课。

---

## 核心功能与特色（v1.6）

1. **🖥️ 宽屏舒适界面与自适应布局（全新优化）**：
   - 窗口拓宽至 `1120x800`，参数栏与状态卡片间距舒适宽敞，彻底消除拥挤挤压感。
   - 自动居中显示，完美适配 Windows 高 DPI 缩放与各类分辨率屏幕。

2. **▶ 4 大智能播放识别模式（深度升级）**：
   - **智能综合识别 (推荐)**：融合中心同心圆盘/圆角矩形底座、多尺度模板匹配与广域播放三角拓扑。
   - **视频中心大播放键**：限定聚焦于屏幕中央视频区域（18%~82%），专门锁定大圆盘/播放图标，免疫一切侧边文字与按键干扰。
   - **纯几何免模板**：全屏扫描播放三角形 ▶，覆盖视频中心大按键与播放器底部控制栏，零模板依赖。
   - **模板匹配优先**：优先使用外部截图模板 `play_button.png` 多尺度边缘匹配。

3. **🎙️ 系统音频内录（默认关闭，可按需开启）**：
   - **默认不开启录制**：避免不需要录音的用户默认占用系统音频资源。
   - **按需开启后智能联动**：
     - 当播放按钮被点击（或视频开始播放）时，**自动启动音频录制**；
     - 当任务完成点击「下一个」切课时，**自动停止并安全保存本节音频**；
     - 切换到新课程视频开始播放时，**自动开启新一节录音**；
     - 点击「停止监控」时，**自动保存正在录制的音频**。
   - **命名规范**：
     - 自动保存到 `recordings/` 目录下，文件命名格式为：
       `recordings/课程录音_第01节_20260824_163015.wav`
     - 采用标准无损 PCM 16-bit 44.1kHz 双声道 WAV 格式。
   - **一键查看**：界面提供「📁 打开录音目录」按钮，一键在资源管理器中打开录音文件夹。

4. **🎯 全场景双轨智能切课定位（彻底杜绝误点「上一个」）**：
   - **轨 1: 弹窗模式**：多尺度 (0.75x~1.30x) 检测完成弹窗并在 ROI 内精确定位「下一个」；
   - **轨 2: 独立按钮模式**：视频播放完毕页面独立亮起「下一个」时，全局多尺度高置信度锁定。

---

## 界面参数与设置

| 配置项 | 推荐值 | 功能说明 |
|---|---|---|
| **检测间隔** | `4~6 秒` (默认 4) | 主监控循环检测频率 |
| **弹窗匹配阈值** | `0.75~0.85` (默认 0.78) | 弹窗相似度门槛（支持多尺度缩放自适应） |
| **按钮匹配阈值** | `0.78~0.85` (默认 0.80) | 「下一个」按钮相似度门槛（防误点「上一个」） |
| **播放检测冷却** | `15 秒` | 播放按钮点击防抖冷却 |
| **自动播放** | 勾选 (开) | 自动识别并点击视频播放按钮 |
| **播放模式** | `智能综合识别 (推荐)` | 可切换：视频中心大播放键 / 纯几何免模板 / 模板匹配优先 |
| **🎙️ 录制课程音频** | 默认不勾选 (关) | **可开关**：勾选后启用系统扬声器声音内录，按小节自动切分保存 |
| **📁 打开录音目录** | 按钮 | 一键在文件管理器中打开 `recordings/` 录音文件夹 |

---

## 文件目录结构

```
dist/
├── 刷课脚本.exe              # [主程序] 独立免环境可执行程序
├── templates/               # [模板目录] 可随时补充外部截图模板
│   ├── task_complete.png    # 弹窗模板
│   ├── next_button.png      # 「下一个」按钮模板
│   └── next_button_wide.png # 宽按钮模板
├── recordings/              # [录音目录] 录制的各节课程音频自动保存在此
└── README.md                # [使用手册] 详细说明文档
```